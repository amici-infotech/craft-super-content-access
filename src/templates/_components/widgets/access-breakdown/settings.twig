{% import '_includes/forms.twig' as forms %}

{{ forms.selectField({
    label: 'Chart'|t('super-content-access'),
    instructions: 'Choose which breakdown to show.'|t('super-content-access'),
    id: 'series',
    name: 'series',
    value: widget.series,
    options: [
        { label: 'Policy location (elements vs defaults)'|t('super-content-access'), value: 'scope' },
        { label: 'Audience type (groups vs users)'|t('super-content-access'), value: 'principals' },
    ],
}) }}
